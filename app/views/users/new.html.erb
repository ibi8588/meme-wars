<div id="clearance" class="sign-up">
  <h2><%= t(".title") %></h2>

  <%= form_for @user do |form| %>
    <%= render partial: '/users/form', object: form %>

    <div class="submit-field">
      <br><%= form.submit class: 'btn'%><br>
    </div>

    <div class="other-links">
      <br><%= link_to t(".sign_in"), sign_in_path %><br>
    </div>
  <% end %>
</div>
